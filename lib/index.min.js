/** @format */

'use strict';
var _extends =
  Object.assign ||
  function(a) {
    for (var b, c = 1; c < arguments.length; c++)
      for (var d in ((b = arguments[c]), b))
        Object.prototype.hasOwnProperty.call(b, d) && (a[d] = b[d]);
    return a;
  };
Object.defineProperty(exports, '__esModule', { value: !0 });
(exports.initializeState = initializeState),
  (exports.getState = getState),
  (exports.updateState = updateState),
  (exports.addListener = addListener);
var state = {},
  updateListeners = [];
function initializeState(a) {
  state = _extends({}, a);
}
function getState() {
  return state;
}
function updateState(a) {
  (state = _extends({}, state, a)),
    updateListeners.forEach(function(a) {
      a();
    });
}
function addListener(a) {
  return (
    updateListeners.push(a),
    function() {
      updateListeners.splice(updateListeners.indexOf(a), 1);
    }
  );
}
